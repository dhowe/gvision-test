<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Index</title>
</head>

<body>
  <svg width="<%= image.width %>" height="<%= image.height %>">
    <g>
      <image href="<%= image.path %>" x="0" y="0" width="100%" height="100%" />
      <% for (let i=0; i < rects.length; i++) { %>
        <rect x="<%= rects[i].x %>" y="<%= rects[i].y %>" width="<%= rects[i].width %>" height="<%= rects[i].height %>"
          style="fill:none; stroke-width: 2px; stroke:rgb(0,255,0)" />
        <rect x="<%= rects[i].x %>" y="<%= rects[i].y %>" fill="rgb(0,255,0)" width="<%= rects[i].width %>"
          height="15" />
        <text fill="#000" x="<%= rects[i].x +5 %>" y="<%= rects[i].y+13 %>" font-size="12" font-family="verdana">
          <%= rects[i].name %>
            <%= Math.round(rects[i].score * 100)+"%" %>
        </text>
      <% } %>
    </g>
  </svg>
  <!-- <%= JSON.stringify(rects) %><br> -->
</body>

</html>